# =============================================================================
# .gitignore for hummbl-dev/codex-agent-folder (HOME directory repo)
# Strategy: DENY ALL, then allowlist only scaffold files.
# Rationale: prevent accidental commits of home directory clutter or agent stacks.
# =============================================================================

# =============================================================================
# STEP 1: IGNORE EVERYTHING by default
# =============================================================================
/*
/.*

# =============================================================================
# STEP 2: ALLOWLIST — only these files/dirs are tracked
# =============================================================================

# Repo markers + core docs
!/.gitignore
!/.REPO_AUTHORIZED
!/.NO_GIT_REPO
!/AGENTS.md
!/README.md
!/LICENSE

# Optional top-level governance docs (ok if absent)
!/EXECUTION_AUTHORITY_PROTOCOL.md
!/AGENT_BIRTH_PROCESS.md
!/AGENT_BIRTH_LOG_TEMPLATE.md
!/VALIDATION_GUIDE.md
!/FREE_AI_STRATEGY.md
!/FOLDER_MANIFEST.md
!/QUICK_START.md

# Entry scripts / operational tooling
!/bin/
!/bin/**

# Maintenance scripts (not symlink)
!/scripts/
!/scripts/*.sh

# Tracked artifacts (scaffold-owned, non-secret)
!/artifacts/
!/artifacts/**

# Templates / scaffolds
!/templates/
!/templates/**

# =============================================================================
# STEP 3: ALWAYS IGNORE — even if allowlisted dirs contain these
# =============================================================================

# Secrets (absolute rules — never track regardless of location)
**/.env
**/.env.*
**/*.secret.*
**/*.key
**/*.pem
**/*.p12
**/*.pfx
**/*.der
**/*.csr
**/*.jks
**/credentials
**/credentials.json
**/token.json
**/*.token
**/.secrets
**/secrets/

# Build artifacts
**/node_modules/
**/dist/
**/build/
**/__pycache__/
**/*.pyc

# OS/editor junk
**/.DS_Store
**/*.swp
**/*.swo
**/*~
**/.viminfo

# Shell histories
**/.bash_history
**/.zsh_history*
**/.python_history
**/.lesshst
